// open-next.config.mjs
import { defineConfig } from '@opennextjs/cloudflare';

export default defineConfig({
  // Defaults are fine for a standard Next.js 15 app using the app router.
  // You can add overrides later if needed.
});